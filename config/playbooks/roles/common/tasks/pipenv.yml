---

# No bionic available yet...
#- name: Add pypa apt repository for pipenv
#  apt_repository:
#      repo: ppa:pypa/ppa
#  become: yes
#
#- name: Install pipenv
#  apt:
#    name:
#      - software-properties-common
#      - python-software-properties
#      - pipenv
#    state: latest
#  become: yes

- name: Install pipenv
  shell: 'python3.7 -m pip install pipenv --target {{ python_user_base_dir.stdout }} --upgrade'
  become: yes
