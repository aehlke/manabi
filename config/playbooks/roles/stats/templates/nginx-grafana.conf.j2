# {{ ansible_managed }}

server {
    listen 80;

    server_name {{ grafana.hostname }};
    charset     utf-8;

    location / {
        proxy_pass http://localhost:{{ grafana.oauth2_proxy_port }};
    }
}
